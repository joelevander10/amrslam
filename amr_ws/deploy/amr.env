# Environment for amr.service. Boot always enters IDLE; no map, survey,
# commissioning job or route is selected or replayed here.
# Which vehicle (profiles/<name>.json): agv-01 = gvievo-01 (BLV-R, CAN drives),
# amr-qr-01 = the AMR QR (analog drives, CANopen encoders, WitMotion IMU).
AGV_PROFILE=agv-01
AMR_MAPS_DIR=/home/gvipc-evo-01/amr_maps
AMR_STATE_DIR=/home/gvipc-evo-01/.amr
AMR_WEB=true
AMR_FOXGLOVE=true
AMR_LIDAR=true
# AMCL against the map with dynamic areas (trolleys) blanked, on a second map_server
# (navigation_layer blank_dynamic). Keep false until the plain-vs-blanked comparison has
# been measured on the vehicle (dynamic-mapping plan, Increment 4).
AMR_LOC_BLANK_DYNAMIC=false
