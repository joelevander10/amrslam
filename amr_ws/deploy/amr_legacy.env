# Environment for the LEGACY fallback units amr_nav.service / amr_mapping.service
# only (migration window, unified plan §9.3). amr.service uses amr.env and never
# selects a map at boot. Delete this file with the legacy units at retirement.
AMR_MAP_ID=line_section
AMR_MAP_REVISION=latest
AMR_MAPS_DIR=/home/gvipc-evo-01/amr_maps
AMR_FOXGLOVE=true
